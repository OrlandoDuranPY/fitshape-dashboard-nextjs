import {Button} from "@/components/ui/button";
import {Card} from "@/components/ui/card";

export default function custom() {
  return (
    <div>
      <Card>
        <Button variant='secondary'>Custom Page</Button>
      </Card>
    </div>
  );
}
